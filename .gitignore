/target
sample/
GPUCache/
*.log
client/node_modules/
client/dist/
.idea/
.spotify*
dist/
osxcross/
.vscode/
test/
vc_redist.x64.exe
assets/vc_redist.x64.exe
*.dll

perf.data
perf.data.old
flamegraph.svg

# Kept as a guard, not because anything here reads one: build and deploy
# config is site-specific and lives with the compose files it describes, not
# in this repo.
.env
.env.local
